# Ruby Rails Docker setup 

## Steps 

* Copy Dockerfile,  docker-compose.yml to your project root directory.
* Create an `.env` file in your project root directory.
* Run `docker-compose up` to start the containers.
